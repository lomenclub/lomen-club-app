# Environment Configuration Example
# Copy this file to .env for local development

# ============================================
# Application Configuration
# ============================================

# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://lomen_db_user:5neoFeBGki4mYlss@lomenclubcluster.drypgap.mongodb.net/lomen-club?retryWrites=true&w=majority&appName=LomenClubCluster

# Application Settings
PORT=3002
NODE_ENV=development

# ============================================
# KCC Blockchain Configuration
# ============================================

# Local Development (recommended for local KCC node)
KCC_RPC_URL=http://localhost:8545
KCC_WS_URL=ws://localhost:8546

# Alternative: Public RPC endpoints (fallback if local node is unavailable)
# KCC_RPC_URL=https://rpc-mainnet.kcc.network
# KCC_WS_URL=wss://rpc-mainnet.kcc.network/ws

# Lomen NFT Contract Address
LOMEN_NFT_CONTRACT=0x4ca64bf392ee736f6007ce93e022deb471a9dfd1
BLOCKCHAIN_PORT=3003

# ============================================
# Redis Cache Configuration
# ============================================

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=lomencache123
REDIS_TTL=300  # 5 minutes cache TTL for blockchain data

# ============================================
# Rate Limiting Configuration
# ============================================

# Conservative limits for public RPC endpoints
RATE_LIMIT_WINDOW_MS=60000  # 1 minute
RATE_LIMIT_MAX_REQUESTS=100  # 100 requests per minute for public RPC

# For local development with local KCC node, you can increase these limits:
# RATE_LIMIT_MAX_REQUESTS=1000

# ============================================
# Frontend Configuration (Vite)
# ============================================

VITE_APP_SHOW_COMING_SOON=true
VITE_APP_COMING_SOON_MESSAGE="Coming Soon - We're working on something amazing!"
VITE_APP_BACKGROUND_COLOR="rgba(0, 0, 0, 0.8)"
VITE_APP_TEXT_COLOR="#ffffff"
VITE_KCC_RPC_URL=https://rpc-mainnet.kcc.network
VITE_KCC_EXPLORER_URL=https://explorer.kcc.io

# ============================================
# Docker Configuration (if running in containers)
# ============================================

# For Docker Desktop on macOS, use host.docker.internal to access host machine
# KCC_RPC_URL=http://host.docker.internal:8545
# KCC_WS_URL=ws://host.docker.internal:8546

# ============================================
# Quick Health Check Commands
# ============================================

# To verify your local KCC node is working:
# 1. Check chainId (should return "0x141"):
#    curl -X POST http://localhost:8545 \
#      -H "Content-Type: application/json" \
#      -d '{"jsonrpc":"2.0","method":"eth_chainId","params":[],"id":1}'
#
# 2. Check sync status (should return "false" when fully synced):
#    curl -X POST http://localhost:8545 \
#      -H "Content-Type: application/json" \
#      -d '{"jsonrpc":"2.0","method":"eth_syncing","params":[],"id":1}'
#
# 3. Check block number:
#    curl -X POST http://localhost:8545 \
#      -H "Content-Type: application/json" \
#      -d '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'
